# UI5 Environment Configuration
# Copy this file to .env and adjust values as needed

# UI5 Environment: cdn | local | backend | hybrid
UI5_ENV=cdn

# Backend server URL (used by ui5-middleware-simpleproxy in backend mode)
# This overrides the baseUri in ui5-backend.yaml
UI5_MIDDLEWARE_SIMPLE_PROXY_BASEURI=http://192.168.1.10:9000

# Development server port
DEV_SERVER_PORT=8300
